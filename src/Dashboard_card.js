import React from "react";
import ReturnCard from "./Dashboard_Card/card";

const DashboardCard = (data) => {
  return (
    <div>
      <ReturnCard data={data} />
    </div>
  );
};

export default DashboardCard;
